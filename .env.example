# Flight Status Board Configuration

# Server Configuration
PORT=8080
NODE_ENV=development

# API Configuration
# Primary API: OpenSky Network (no key required, but has rate limits)
OPENSKY_API_URL=https://opensky-network.org/api/states/all

# Optional: Add API keys here for production use
# AVIATION_STACK_API_KEY=your_key_here
# FLIGHTRADAR24_API_KEY=your_key_here

# API Timeout (milliseconds)
API_TIMEOUT=10000
