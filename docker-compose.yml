version: '3.8'
services:
  flight-status-board:
    build: .
    image: flight-status-board:latest
    ports:
      - '8080:8080'
    environment:
      NODE_ENV: 'production'
      PORT: 8080
    restart: unless-stopped
